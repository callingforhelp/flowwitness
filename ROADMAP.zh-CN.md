# 路线图

[English](ROADMAP.md)

除本地交互演示外，下列运行时能力都处于**计划阶段**，排序表达依赖关系，不承诺日期。

## 已有基础

- 产品定义、带来源的桌面调研及试用指标。
- 中英文项目网站、可操作的合成状态演示与文档。
- 工作流/接口样例、MIT 许可证及贡献规范。

## 第一阶段：少量关键 Web 流程的可靠指引

| 顺序 | 工作 | 验收证据 |
| --- | --- | --- |
| 1 | 流程格式和 CLI 初始化、校验、列表 | 三个样例往返一致；拒绝悬空 ID、缺少结果/版本和私密资源 |
| 2 | 在有种子数据的本地网页捕获与回放 | 正常通过；移动按钮后失败；审核修正后通过；保留步骤证据 |
| 3 | Git 影响分析与部署绑定 | 关联改动触发；共用权限触发相关流程；无关文件不误报；预览不覆盖生产 |
| 4 | 问题队列与发布规则 | 同一版本只产生一个待办；缺少必要事实时不能发布 |
| 5 | 查询接口与可选图片输入 | 已知流程给证据；失效/未知/错角色/模糊图片不输出错误步骤 |
| 6 | 共用技能入口和结构化指引 | 先验证一个宿主，再分别给 Claude Code/Codex/pi 兼容性证据 |
| 7 | 真实试用 | 三位开发者、两次发布，记录质量、维护成本与隐私测试 |

先用 Node/TypeScript、Git JSON 文件、本地证据目录、一个浏览器适配器和小型 HTTP 服务。实现时确认具体运行时版本。Playwright 负责可重复断言，Stagehand 可作为语义定位/恢复适配器；本地名为 sagehand 的工具不等于可分发的标准依赖。

发布门槛是记录 → 改动 → 重验 → 查询在真实试用部署形成闭环，达到 [产品定义](docs/PRODUCT.zh-CN.md) 中的可测目标。只有本地样例通过不代表生产就绪。

## 第二阶段：在证明价值后扩展

1. 短视频：把成功回放整理成字幕步骤，再考虑配音；按流程/版本/角色复用，链接可过期。原始录屏不等于成品教程。
2. 更强图像理解：裁剪、模糊、多语言、旧界面；不能隐藏版本和角色不确定性。
3. 用户侧电脑操作：按宿主协议转换步骤，检查前置条件、请求必要确认、支持取消、遇到不符停止并验证完成。
4. 更多沙箱场景：权限、网络、功能开关和恢复路径；先限额队列，再考虑多代理。
5. 原生插件与关系图：宿主事件钩子、可选 MCP 包装、需要时再做 Archify 导出。

根据试用中真正阻碍完成任务的问题调整顺序，不添加聊天产品、市场、图数据库或常驻客户浏览器。
