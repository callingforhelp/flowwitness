# 路线图

[English](ROADMAP.md)

本地 alpha 已实现核心运行时；认证的托管试点后端和具备浏览器能力的共用 EC2
公开路由演练已可供赞助方审阅，公开 Pages 概念演示仍是模拟，实际验证请运行操作台。
外部试用与生产认证尚未验证。排序表达依赖，不承诺日期。

## 已有基础

- 产品定义、带来源的桌面调研及试用指标。
- 中英文项目网站、可操作的合成状态演示与文档。
- 工作流/接口样例、MIT 许可证及贡献规范。

## 第一阶段：试点核心

第一阶段是应该交给首批试点用户的产品：开发者记录流程，浏览器验证产生
证据，代码改动标记旧指引失效，已有客服聊天再查询当前已发布的答案。

| 顺序 | 工作 | 验收证据 |
| --- | --- | --- |
| 1 — 已实现 | 流程格式和 CLI 初始化、校验、列表 | 三个样例往返一致；拒绝悬空 ID、缺少结果/版本和私密资源 |
| 2 — 明确记录和真实回放已实现 | 在有种子数据的本地网页捕获与回放 | 正常通过；移动按钮后失败；审核修正后通过；保留步骤证据 |
| 3 — 已实现 | Git 影响分析与部署绑定 | 关联改动触发；共用权限触发相关流程；无关文件不误报；预览不覆盖生产 |
| 4 — 已实现 | 问题队列与发布规则 | 同一版本只产生一个待办；缺少必要事实时不能发布 |
| 5 — 已实现，无视觉识别 | 查询接口与可选图片输入 | 已知流程给证据；失效/未知/错角色/模糊图片不输出错误步骤 |
| 6 — 技能已提供，宿主测试待完成 | 共用技能入口和结构化指引 | 先验证一个宿主，再分别给 Claude Code/Codex/pi 兼容性证据 |
| 7 — 公开演练完成，外部待完成 | 真实试用 | [试点 02](docs/PILOT-02.zh-CN.md)记录了公开合成两版本收据；剩余门槛是三位伙伴、产品指标与隐私测试 |

第一阶段后端包括工作流服务、问题与消息、中英文推理库、代理注册与任务租约、
本地持久化、模块 CLI 和中英文操作台。首批试点不依赖其它阶段。

当前使用 Node.js 22+ ESM JavaScript、Git JSON 文件、私密证据目录、Playwright 和原生 HTTP 服务，不需要 TypeScript 构建或模型密钥。Playwright 负责可重复断言，Stagehand 可作为语义定位/恢复适配器；本地名为 sagehand 的工具不等于可分发的标准依赖。

第二阶段模块已经在本地服务中隔离组合，因此可以单独测试而不改变第一阶段闭环，
包括批准的预览/测试复现和私有 FFmpeg 视频编辑。它们仍需试点门槛：默认不启用
E2B 浏览器供应商、面向客户的视频交付或自动操作客户电脑。

发布门槛是记录 → 改动 → 重验 → 查询在真实试用部署形成闭环，达到 [产品定义](docs/PRODUCT.zh-CN.md) 中的可测目标。试点 02 证明了合成数据的公开路由；本地或合成样例通过不代表外部试点就绪。

## 第二阶段：证明闭环价值后再扩展

1. 演示视频交付：验证已有私有 FFmpeg 工作室，配置渲染器/字体，再加入客户交付和可选配音。按流程/版本/角色复用，不为每个问题生成视频。
2. 预览复现：将已有的有界复现模块接入获得批准的 E2B 或同类浏览器适配器，测试权限、网络失败、功能开关和恢复路径。
3. 更强图像理解：裁剪、模糊、多语言、旧界面；不能隐藏版本和角色不确定性。
4. 用户侧电脑操作：按宿主协议转换步骤，检查前置条件、请求必要确认、支持取消、遇到不符停止并验证完成。
5. 原生插件与关系图：宿主事件钩子、可选 MCP 包装、只有维护者确实需要关系检查时再做 Archify 导出。

根据试用中真正阻碍完成任务的问题调整顺序，不添加聊天产品、市场、图数据库或常驻客户浏览器。第二阶段代码保留在独立模块中；推迟某个阶段不等于删除它。
